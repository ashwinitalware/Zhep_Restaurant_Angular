import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Zhep_Restaurant_Angular',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
